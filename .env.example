# Copy this file to .env for local testing with `vercel dev` (optional).
# In production, set this in your Vercel project's Settings -> Environment Variables instead.
# Get a free key (no credit card needed) at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_key_here
